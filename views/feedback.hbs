{{> header user=user}}

<main class="feedback-page">
<<<<<<< Updated upstream
  <h2>Send Feedback</h2>
=======
  <div class="feedback-container">
    <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 1.5rem;">
      <h2 style="margin: 0;">Send Feedback</h2>
      <a href="/chat" class="btn btn-outline-primary" style="display: inline-flex; align-items: center; gap: 0.5rem;">
        <i class="bi bi-arrow-left"></i> Back to Chat
      </a>
    </div>
>>>>>>> Stashed changes

  {{#if error}}
    <div class="alert alert-error">{{error}}</div>
  {{/if}}

  {{#if success}}
    <div class="alert alert-success">{{success}}</div>
  {{/if}}

  <form method="POST" action="/feedback">
    <label>Message</label>
    <textarea name="message" rows="4" required>{{message}}</textarea>

    <label>Rating (optional)</label>
    <select name="rating">
      <option value="">Select rating</option>
      {{#each [1,2,3,4,5]}}
        <option value="{{this}}">{{this}}</option>
      {{/each}}
    </select>

    <button type="submit">Submit Feedback</button>
  </form>
</main>

{{> footer}}
